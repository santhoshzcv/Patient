import { CustomEqualValidatorDirective } from './custom-equal-validator.directive';

describe('CustomEqualValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomEqualValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
